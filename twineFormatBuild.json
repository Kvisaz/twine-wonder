{
  "src": {
    "format": "src/wonder-format/wonderFormat.js",
    "script": "dist/${version}/wonder/${bundleScript}",
    "style": ""
  },
  "output": "format/wonder.${version}.js",
  "marker": {
    "style": "${style}",
    "script": "${script}",
    "version": "${version}",
    "author": "${author}",
    "name": "${name}",
    "nameFormat": "${nameFormat}",
    "repository": "${repository}",
    "description": "${description}",
    "icon": "${icon}",
    "bundleScript": "${bundleScript}"
  }
}
